"use client"; // This page needs client-side rendering for ApplicationForm

import ApplicationForm from "@/components/ApplicationForm";

export default function AddApplication() {
  return <ApplicationForm />;
}